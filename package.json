{
  "name": "ripples",
  "version": "0.1.0",
  "description": "Add a water ripple effect to background images using WebGL.",
  "keywords": [
    "water",
    "ripples",
    "WebGL",
    "background",
    "effect"
  ],
  "scripts": {
    "build": "rollup -c build/rollup.config.js",
    "build-min": "rollup -c build/rollup.config-min.js",
    "version": "npm run build && npm run build-min && git add -A dist",
    "postversion": "git push && git push --tags"
  },
  "homepage": "https://github.com/gsbm/ripples",
  "license": "MIT",
  "author": {
    "name": "gsbm",
  },
  "main": "dist/jquery.ripples.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/gsbm/ripples.git"
  },
  "devDependencies": {
    "rollup": "^0.45.1",
    "rollup-plugin-license": "^0.6.0",
    "rollup-plugin-uglify": "^2.0.1"
  },
  "dependencies": {},
  "files": [
    "dist",
    "README.md"
  ]
}
